{
  "name": "blogpost",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup:install": "cd api && npm install && cd ../frontend && npm install",
    "setup:backend": "cd api && npm start",
    "setup:frontend": "cd frontend && npm start",
    "setup:start": "concurrently \"npm run setup:backend\" \"npm run setup:frontend\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
